    <% if(user_signed_in?) %>
      <p>Welcome,<%= current_user.name %>&nbsp;(<%= current_user.role %>)<br/></p>
      <h1>Your Chats</h1>
    <% end %>

<div class="row">
  <div class="col-sm-3">
    <ul class="nav nav-pills nav-stacked">
      <%= mailbox_section 'inbox', @box %>
    </ul>
  </div>

  <div class="col-sm-9">
    <ul class="list-group">
      <%= render partial: 'conversations/conversation', collection: @conversations %>
    </ul>

    <%= will_paginate %>
  </div>
</div>